MODULE lib

PRIVATE

PUBLIC

# pop (a -- )
# prints a to stdout and then drops it
pop 1:0 == print drop;

# neq (a b -- a != b)
# returns 1 if a != b otherwise returns 0
neq 2:1 == eq not;

# booleans
true 0:1 == 1;
false 0:1 == 0;

END